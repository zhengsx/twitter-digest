# Twitter Digest 📰

每日自动追踪 Twitter 信源，生成 AI 分析报告。

## 功能

- 📋 获取指定用户的关注列表作为信源
- 📥 每日爬取信源的推文和评论
- 🤖 使用 Gemini 3 Pro 生成智能日报
- 📱 自动发送 Telegram 通知

## 安装

```bash
cd ~/Documents/Projects/twitter-digest
npm install
```

## 配置

1. 复制环境变量模板：
```bash
cp .env.example .env
```

2. 编辑 `.env` 填入：
- `TWITTER_BEARER_TOKEN` - Twitter API Bearer Token
- `TARGET_USERNAME` - 目标用户 (默认 xxcc48764)
- `OPENROUTER_API_KEY` - OpenRouter API Key
- `TELEGRAM_BOT_TOKEN` - Telegram Bot Token (可选)
- `TELEGRAM_CHAT_ID` - Telegram Chat ID (可选)

## 获取 Twitter API

1. 访问 https://developer.twitter.com/
2. 创建 Project 和 App
3. 生成 Bearer Token

## 使用

### 手动运行
```bash
npm run daily
```

### 设置每日定时任务 (cron)
```bash
# 每天早上 9 点运行
0 9 * * * cd ~/Documents/Projects/twitter-digest && npm run daily >> logs/cron.log 2>&1
```

### 用 OpenClaw 定时
可以在 OpenClaw 中设置 cron job 自动执行。

## 目录结构

```
twitter-digest/
├── src/
│   ├── config.js           # 配置
│   ├── twitter-client.js   # Twitter API 封装
│   ├── report-generator.js # Gemini 报告生成
│   ├── telegram-notifier.js# Telegram 通知
│   └── daily-job.js        # 主程序
├── data/                   # 原始数据
├── reports/                # 生成的报告
├── .env                    # 环境变量
└── package.json
```

## 报告示例

报告会保存在 `reports/report-YYYY-MM-DD.md`，包含：
- 今日热点话题
- 关键人物发言
- 行业趋势分析

---

🦞 Generated by sx的小龙虾
